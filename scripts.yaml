notify_on_telegram:
  sequence:
  - service: notify.telegram_ana
    data_template:
      message: >
          {{ title }}

          {{ message }}
  - service: notify.telegram_mateus
    data_template:
      message: >
          {{ title }}

          {{ message }}
